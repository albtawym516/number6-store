# NUMBER 6 - متجر إلكتروني احترافي متكامل

متجر إلكتروني احترافي للملابس مع لوحة تحكم كاملة ونظام دفع عند الاستلام.

## ✨ الميزات الرئيسية

- **نظام دفع عند الاستلام:** لا يتطلب بطاقات ائتمانية، الدفع عند استلام المنتج
- **قاعدة بيانات:** MongoDB أو بيانات وهمية للتطوير
- **نظام إشعارات:** إرسال بريد إلكتروني تلقائي عند استلام الطلبات
- **لوحة تحكم آمنة:** إدارة كاملة للمنتجات والطلبات محمية بعنوان IP
- **واجهة متجاوبة:** تعمل على جميع الأجهزة
- **جاهز للنشر:** إعدادات كاملة للنشر على الإنترنت

## 🛠️ التقنيات المستخدمة

- **الواجهة الأمامية:** React, TypeScript, Tailwind CSS, React Router
- **الخادم:** Node.js, Express, Mongoose (MongoDB), Nodemailer
- **قاعدة البيانات:** MongoDB (اختياري)
- **أدوات التطوير:** Vite, ESLint, Nodemon, Concurrently

## 🚀 كيفية تشغيل المشروع

### المتطلبات المسبقة
- Node.js (v18 أو أحدث)
- npm (يأتي مع Node.js)
- MongoDB (اختياري للتطوير)

### 1. تثبيت الاعتماديات
```bash
npm install
```

### 2. التشغيل المحلي
```bash
npm run dev
```
- الواجهة الأمامية: `http://localhost:5173`
- الخادم: `http://localhost:3001`

### 3. الوصول للتطبيق
- **المتجر:** `http://localhost:5173`
- **لوحة التحكم:** `http://localhost:5173/#/admin`
  - **كلمة المرور:** `01206155419`

## 🌐 النشر على الإنترنت

### خطوات سريعة:
1. **إعداد البيئة:**
   ```bash
   cp .env.production .env
   ```

2. **تعديل ملف .env:**
   - أضف عنوان IP الخاص بك إلى `ALLOWED_ADMIN_IPS`
   - اضبط إعدادات البريد الإلكتروني
   - استخدم MongoDB حقيقي للإنتاج

3. **البناء والتشغيل:**
   ```bash
   npm run build
   npm run start:prod
   ```

4. **إعداد Reverse Proxy (Nginx) و Process Manager (PM2)**

### الأمان في الإنتاج:
- لوحة التحكم محمية بقائمة بيضاء لعناوين IP
- فقط أنت تستطيع الوصول للوحة التحكم
- كلمة مرور لوحة التحكم: `01206155419`

## 📧 الإشعارات البريدية

يمكن تفعيل إشعارات البريد الإلكتروني للطلبات الجديدة عبر:
- إعداد حساب Gmail في ملف `.env`
- تفعيل كلمة مرور التطبيقات

## 🔧 التخصيص

### تغيير كلمة مرور لوحة التحكم:
في ملف `server/server.js`، ابحث عن:
```javascript
if (password === '01206155419') {
```
واستبدلها بكلمة المرور الجديدة.

### إضافة منتجات:
- عبر لوحة التحكم
- أو تعديل ملف `data/mockDataServer.js`

## 📋 ملاحظات هامة

- المشروع يستخدم نظام الدفع عند الاستلام (لا يوجد Stripe)
- يمكن تشغيله بدون MongoDB باستخدام البيانات الوهمية
- جاهز تماماً للنشر على استضافة VPS أو Cloud

## 📖 الدليل الكامل

انظر ملف `DEPLOYMENT.md` للخطوات التفصيلية للنشر على الإنترنت.
